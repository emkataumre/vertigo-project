{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f package.json ] && npm test -- --watchAll=false 2>&1 || exit 0"
          }
        ]
      }
    ]
  }
}